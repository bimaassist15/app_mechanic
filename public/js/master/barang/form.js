select2Standard({
    parent: "#extraLargeModal",
    selector: "select[name='kategori_id']",
});
select2Standard({
    parent: "#extraLargeModal",
    selector: "select[name='satuan_id']",
});
select2Standard({
    parent: "#extraLargeModal",
    selector: "select[name='nonsn_or_sn']",
});
